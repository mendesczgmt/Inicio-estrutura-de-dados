from Estatica import les

lista = les.les()

if not (lista.cheia()):
    lista.inserirFim(1)
if not (lista.Cheia()):
    lista.inserirFim(2)
if not (lista.Cheia()):
    lista.inserirFim(3)
if not (lista.Cheia()):
    lista.inserirFim(4)
if not (lista.Cheia()):
    lista.inserirFim(1)
if not (lista.Cheia()):
    lista.inserirFim(6)
print('Lista com insercao dos elementos 1, 2, 3, 4 e 5 no fim da lista e tentativa do 6')
lista.imprimir()